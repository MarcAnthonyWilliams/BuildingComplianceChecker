{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },

  "scripts":{
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/compliance-checker\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend/compliance-checker"
  }
}

